@using ESD6NL.DriverSystem.Entities
@model ESD6NL.DriverSystem.Models.RowModel
@{
    ViewData["Title"] = "Map";
}

<h2>Map</h2>


LOOK AT THIS BOOTYFULL MAP; DEEZ R TEH POINT DRIVEN AS WELL SO NAU U CAN VISUALISE IT
@foreach (RoutePoint r in @Model.route)
{
    <span>Lat = @r.Lat, long = @r.Lon</span>

}